# smithery.yaml
# MCP server configuration for Smithery deployment

name: mlb_mcp
version: 0.1.0
description: MCP server for MLB and Fangraphs data using pybaseball and FastAPI
start: uvicorn main:app --host 0.0.0.0 --port 8080 --log-config uvicorn_log_config.json
port: 8080
dockerfile: Dockerfile
